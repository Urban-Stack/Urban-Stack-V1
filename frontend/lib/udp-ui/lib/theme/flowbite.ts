/* v8 ignore start */
import { createTheme } from 'flowbite-react';

export const flowbiteTheme = createTheme({
  table: {
    body: {
      base: 'group/body divide-y divide-gray-100',
    },
  },
} as const);
