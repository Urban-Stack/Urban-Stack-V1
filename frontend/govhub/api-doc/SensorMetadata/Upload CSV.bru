meta {
  name: Upload CSV
  type: http
  seq: 4
}

post {
  url: {{sensormeta-host}}/api/v1/metadata/{{tenant}}/{{project}}
  body: file
  auth: inherit
}

body:file {
  file: @file(_examples/sensormeta.csv) @contentType(text/csv; charset=utf-8)
}
