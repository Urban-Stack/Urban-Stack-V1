meta {
  name: Delete dashboard
  type: http
  seq: 12
}

delete {
  url: {{keycloak-host}}/realms/{{realm}}/data-hub/tenants/{{tenant}}/viz-groups/{{viz-group}}/dashboards/{{dashboard-name}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access-token}}
}
